import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background surfaces
  static const Color background = Color(0xFF0D0D1A);
  static const Color surface = Color(0xFF12122A);
  static const Color surfaceDark = Color(0xFF0A0A18);
  static const Color surfaceElevated = Color(0xFF1A1A2E);
  static const Color card = Color(0xFF1E1E32);

  // Accent colors
  static const Color primary = Color(0xFF6C63FF);
  static const Color primaryLight = Color(0xFF8B85FF);
  static const Color primaryDark = Color(0xFF4A44CC);
  static const Color secondary = Color(0xFF03DAC6);
  static const Color tertiary = Color(0xFFFFB74D);

  // Text colors
  static const Color textPrimary = Color(0xFFE6E6F0);
  static const Color textSecondary = Color(0xFFA0A0B8);
  static const Color textMuted = Color(0xFF6B6B80);

  // Functional colors
  static const Color error = Color(0xFFCF6679);
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);

  // Recording indicator
  static const Color recordingRed = Color(0xFFE53935);

  // Camera overlay
  static const Color overlaySemiTransparent = Color(0x80000000);
}
